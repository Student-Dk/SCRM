<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CRM Dashboard</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    /* ------------------ Layout ------------------ */
    body {
      display: flex;
      background-color: #f0f3f6;
    }

    .container {
      display: flex;
      width: 100%;
    }

    /* ------------------ Sidebar ------------------ */
    .sidebar {
      width: 250px;
      background-color: #1c1f26;
      color: white;
      min-height: 100vh;
      padding: 20px;
    }

    .logo {
      font-size: 28px;
      font-weight: bold;
      margin-bottom: 30px;
    }

    .profile {
      display: flex;
      align-items: center;
      margin-bottom: 30px;
    }

    .profile img {
      width: 50px;
      border-radius: 50%;
      margin-right: 10px;
    }

    .welcome {
      font-size: 14px;
    }

    .status {
      display: block;
      font-size: 12px;
      color: #00e676;
    }

    /* ------------------ Navigation ------------------ */
    .nav a {
      display: flex;
      align-items: center;
      padding: 10px 0;
      color: #ccc;
      text-decoration: none;
      transition: 0.3s;
    }

    .nav a:hover {
      color: #fff;
    }

    .nav i {
      margin-right: 10px;
    }

    /* ------------------ Main Content ------------------ */
    .main-content {
      flex-grow: 1;
      padding: 40px;
      background-color: #f4f7fa;
      min-height: 100vh;
    }

    .main-content p {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 30px;
    }

    /* ------------------ Form Styles ------------------ */
    .main-content form {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 8px;
      max-width: 500px;
      box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1);
    }

    .main-content form label {
      display: block;
      margin-bottom: 6px;
      font-weight: 500;
      color: #333;
    }

    .main-content form .form-group {
      margin-bottom: 20px;
    }

    .input-wrapper {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    /* ------------------ Input Styles ------------------ */
    .main-content form input {
      width: 100%;
      padding: 10px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 5px;
      outline: none;
      background-color: #e9ecef;
      transition: 0.3s ease;
    }

    .main-content form input:focus {
      border-color: #007bff;
      box-shadow: 0 0 5px rgba(0, 123, 255, 0.3);
      background-color: #fff;
    }

    /* ------------------ Buttons ------------------ */
    .main-content form button {
      padding: 10px 20px;
      margin-right: 10px;
      background-color: #007bff;
      color: white;
      font-size: 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .main-content form button:hover {
      background-color: #0056b3;
    }

    .main-content #editBtn {
      margin-bottom: 20px;
      background-color: #28a745;
    }

    .main-content #editBtn:hover {
      background-color: #1e7e34;
    }

    .main-content #clearBtn {
      background-color: #dc3545;
    }

    .main-content #clearBtn:hover {
      background-color: #c82333;
    }

    /* ------------------ Inline Edit Buttons ------------------ */
    .edit-btn {
      background-color: #ffc107;
      border: none;
      padding: 7px 12px;
      color: #000;
      border-radius: 4px;
      cursor: pointer;
      font-size: 14px;
    }

    .edit-btn:hover {
      background-color: #e0a800;
    }
  </style>
</head>

<body>
  <div class="container">
    <%- include('./navbar') %>
    <!-- <aside class="sidebar">
      <div class="logo">CRM</div>
      <div class="profile">
        <img src="https://www.w3schools.com/howto/img_avatar.png" alt="Avatar" />
        <div class="welcome">
          <p><strong>Welcome</strong><br />
            <%= user.name %>
          </p>
          <span class="status online">● Online</span>
        </div>
      </div>
      <nav class="nav">
        <a href="/dashboard/<%=user._id%>"><i class="fas fa-home"></i> Dashboard</a>
        <form action="/change_password" method="post">
          <input type="hidden" name="id" value="<%=user._id%>">
          <button type="submit">change password</button>
        </form>
        <a href="/profile/<%=user._id%>">Profile</a>
        <a href="#"><i class="fas fa-file-alt"></i> Request a Quote</a>
        <a href="#"><i class="fas fa-pen"></i> Create Ticket</a>
        <a href="#"><i class="fas fa-eye"></i> View Ticket</a>
      </nav>
    </aside> -->

    <main class="main-content">
      <p>
        <%= user.name %> Profile
      </p>

      <!-- EDIT BUTTON -->
      <form  action="/updateprofile" method="post">
        <input type="hidden" value="<%= user._id %>" name="id">
        <div class="form-group">
          <label>Name</label>
          <div class="input-wrapper">
            <input type="text" name="name" id="name" value="<%=user.name%>" >
            <!-- <button type="button" class="edit-btn" onclick="enableEdit('name')">Edit</button> -->
          </div>
        </div>

        <div class="form-group">
          <label>Email</label>
          <div class="input-wrapper">
            <input type="email" name="email" id="email" value="<%=user.email%>" >
            <!-- <button type="button" class="edit-btn" onclick="enableEdit('email')">Edit</button> -->
          </div>
        </div>

        <div class="form-group">
          <label>Contact</label>
          <div class="input-wrapper">
            <input type="text" name="contact" id="contact" value="<%=user.contact%>" >
            <!-- <button type="button" class="edit-btn" onclick="enableEdit('contact')">Edit</button> -->
          </div>
        </div>

        <div class="form-group">
          <label>Address</label>
          <div class="input-wrapper">
            <input type="text" name="address" id="address" value="<%=user.address%>">
            <!-- <button type="button" class="edit-btn" onclick="enableEdit('address')">Edit</button> -->
          </div>
        </div>

        <button  type="submit" onclick="alert('your profile is updated')">Update</button>
        <button type="button" onclick="clearForm()">Clear</button>
      </form>

    </main>
  </div>

  <!-- JavaScript -->
  <!-- <script>
    function enableEdit(fieldId) {
      const input = document.getElementById(fieldId);
      input.disabled = false;
      input.focus();
    }

    function clearForm() {
      document.getElementById("profile-form").reset();
      const inputs = document.querySelectorAll("#profile-form input");
      inputs.forEach(input => input.disabled = true);
    }
  </script> -->

</body>

</html>